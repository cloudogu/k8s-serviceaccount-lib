---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: serviceaccountproducers.k8s.cloudogu.com
  labels:
    app: ces
    app.kubernetes.io/name: k8s-serviceaccount-lib
spec:
  group: k8s.cloudogu.com
  names:
    kind: ServiceAccountProducer
    listKind: ServiceAccountProducerList
    plural: serviceaccountproducers
    shortNames:
      - sapr
    singular: serviceaccountproducer
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: Whether the service account request is ready
          jsonPath: .status.conditions[?(@.type == 'Ready')].status
          name: Ready
          type: string
        - jsonPath: .spec.producer
          name: Producer
          type: string
        - jsonPath: .spec.http.endpoint
          name: Endpoint
          priority: 1
          type: string
        - jsonPath: .spec.exec.command
          name: Command
          priority: 1
          type: string
        - jsonPath: .status.lastExecution
          name: Last Execution
          type: date
        - description: The age of the resource
          jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v2
      schema:
        openAPIV3Schema:
          description: ServiceAccountProducer is the Schema for the serviceaccount request API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired state of ServiceAccountProducer.
              properties:
                exec:
                  description: Exec defines the necessary information to create a service account via k8s exec api.
                  properties:
                    command:
                      description: Command describes the command used to create service accounts.
                      example: /create-sa.sh
                      type: string
                    params:
                      description: Params defines the parameters that should and can be used to create the service account.
                      properties:
                        attributes:
                          additionalProperties:
                            description: AttributeDefinition defines the structure of an attribute used to create service accounts.
                            properties:
                              description:
                                description: Description defines a human-readable description for the attribute.
                                maxLength: 253
                                minLength: 1
                                type: string
                              enum:
                                description: Enum defines the possible values if AttributeDefinition.Type is string.
                                items:
                                  type: string
                                maxItems: 20
                                type: array
                              order:
                                description: Order defines the order of the attribute.
                                type: integer
                              type:
                                default: string
                                description: Type defines the type of the attribute.
                                enum:
                                  - string
                                  - boolean
                                  - integer
                                type: string
                            required:
                              - description
                              - type
                            type: object
                          description: Attributes define the attributes that should be used to create the service account.
                          maxProperties: 20
                          minProperties: 1
                          type: object
                        required:
                          description: Required defines the attributes that are required to create the service account.
                          items:
                            description: AttributeName defines the name of an attribute.
                            type: string
                          type: array
                      required:
                        - attributes
                      type: object
                    priority:
                      description: Priority describes the priority of choosing a way to create the service account if multiple producers a configured.
                      type: integer
                    return:
                      additionalProperties:
                        description: ProducerReturnDefinition defines the structure of the return values from the producer.
                        properties:
                          description:
                            maxLength: 253
                            minLength: 1
                            type: string
                        required:
                          - description
                        type: object
                      description: |-
                        Return defines the structure of the return values from the producer.
                        It is used to write the actual service account data to the secret.
                      type: object
                    selector:
                      description: Selector describes the label selector used to select the pods that should be used to create service accounts.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                              - key
                              - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                    - command
                    - return
                    - selector
                  type: object
                http:
                  description: HTTP defines the necessary information to create a service account via HTTP.
                  properties:
                    authSecret:
                      description: AuthSecret describes the reference of the secret that should be used to create the service account.
                      properties:
                        key:
                          example: auth
                          maxLength: 253
                          minLength: 1
                          pattern: ^[-_.a-zA-Z0-9]+$
                          type: string
                        name:
                          description: Name des Secrets.
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?([.][a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                      required:
                        - key
                        - name
                      type: object
                    endpoint:
                      description: |-
                        Endpoint describes the url used to create service accounts.
                        This endpoint should implement the following verbs: PUT, POST, DELETE to create, update and delete service accounts.
                      example: https://nexus:8081/api/cloudogu/service-account
                      format: uri
                      maxLength: 253
                      minLength: 1
                      type: string
                    params:
                      description: Params defines the parameters that should and can be used to create the service account.
                      properties:
                        attributes:
                          additionalProperties:
                            description: AttributeDefinition defines the structure of an attribute used to create service accounts.
                            properties:
                              description:
                                description: Description defines a human-readable description for the attribute.
                                maxLength: 253
                                minLength: 1
                                type: string
                              enum:
                                description: Enum defines the possible values if AttributeDefinition.Type is string.
                                items:
                                  type: string
                                maxItems: 20
                                type: array
                              order:
                                description: Order defines the order of the attribute.
                                type: integer
                              type:
                                default: string
                                description: Type defines the type of the attribute.
                                enum:
                                  - string
                                  - boolean
                                  - integer
                                type: string
                            required:
                              - description
                              - type
                            type: object
                          description: Attributes define the attributes that should be used to create the service account.
                          maxProperties: 20
                          minProperties: 1
                          type: object
                        required:
                          description: Required defines the attributes that are required to create the service account.
                          items:
                            description: AttributeName defines the name of an attribute.
                            type: string
                          type: array
                      required:
                        - attributes
                      type: object
                    priority:
                      description: Priority describes the priority of choosing a way to create the service account if multiple producers (e.g. http and exec) a configured.
                      type: integer
                    return:
                      additionalProperties:
                        description: ProducerReturnDefinition defines the structure of the return values from the producer.
                        properties:
                          description:
                            maxLength: 253
                            minLength: 1
                            type: string
                        required:
                          - description
                        type: object
                      description: |-
                        Return defines the structure of the return values from the producer.
                        It is used to write the actual service account data to the secret.
                      type: object
                  required:
                    - authSecret
                    - endpoint
                    - return
                  type: object
                producer:
                  description: Producer defines the name of the service account producing dogu or component.
                  example: prometheus
                  maxLength: 253
                  minLength: 1
                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?([.][a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                  type: string
                  x-kubernetes-validations:
                    - message: Producer is immutable
                      rule: self == oldSelf
              required:
                - producer
              type: object
              x-kubernetes-validations:
                - message: At least one producer strategy (http or exec) must be defined
                  rule: has(self.http) || has(self.exec)
            status:
              description: status defines the observed state of ServiceAccountProducer.
              properties:
                conditions:
                  description: Conditions represent the current state of the ServiceAccountProducer resource.
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                lastExecution:
                  description: LastExecution defines the timestamp of the last execution.
                  format: date-time
                  type: string
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
