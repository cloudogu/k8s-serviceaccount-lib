---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: serviceaccountrequests.k8s.cloudogu.com
  labels:
    app: ces
    app.kubernetes.io/name: k8s-serviceaccount-lib
spec:
  group: k8s.cloudogu.com
  names:
    kind: ServiceAccountRequest
    listKind: ServiceAccountRequestList
    plural: serviceaccountrequests
    shortNames:
      - sare
    singular: serviceaccountrequest
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - description: Optional nature of the resource
          jsonPath: .spec.optional
          name: Optional
          type: boolean
        - jsonPath: .spec.consumer
          name: Consumer
          type: string
        - description: Type of the requester
          jsonPath: .spec.consumerType
          name: ConsumerType
          type: string
        - description: Producer of the requester
          jsonPath: .spec.producer
          name: Producer
          type: string
        - description: Actual secret name
          jsonPath: .status.secretRef.name
          name: Secret
          type: string
        - jsonPath: .spec.rotation.enabled
          name: Rotation
          type: boolean
        - jsonPath: .spec.rotation.rotation
          name: Schedule
          type: string
        - jsonPath: .status.lastRotation
          name: Last-Run
          type: date
        - jsonPath: .spec.params
          name: Params
          priority: 1
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v2
      schema:
        openAPIV3Schema:
          description: |-
            ServiceAccountRequest define the structure of a service account request.
            ServiceAccountRequest is the Schema for the serviceaccount request API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired state of ServiceAccountRequest.
              properties:
                consumer:
                  description: Consumer defines the name of the requesting Dogu or Component.
                  example: grafana
                  maxLength: 253
                  minLength: 1
                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?([.][a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                  type: string
                  x-kubernetes-validations:
                    - message: Consumer is immutable
                      rule: self == oldSelf
                consumerType:
                  description: ConsumerType defines the type of the requester. This is necessary for restarting the requester if the account is optional and will be created later.
                  enum:
                    - Dogu
                    - Component
                  example: Dogu
                  type: string
                  x-kubernetes-validations:
                    - message: ConsumerType is immutable
                      rule: self == oldSelf
                optional:
                  default: false
                  description: Optional defines if the request is optional and can be retried if the producer is currently not available.
                  example: false
                  type: boolean
                params:
                  additionalProperties:
                    type: string
                  description: |-
                    Params defines a map of parameters provided (via service account operator) to the service account producer to
                    modify their service account creation behavior (if supported).

                    The number and quality of parameters highly depend on the implementation of the service account producer.

                    With maps being unsorted, the service account operator may provide a parameter sorting by a derived logic that is
                    not being discussed here.
                  maxProperties: 20
                  type: object
                producer:
                  description: Producer defines the producer who should process the service account request. The value has to correspond to the name of the producer resource.
                  example: prometheus
                  maxLength: 253
                  minLength: 1
                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                  type: string
                  x-kubernetes-validations:
                    - message: Producer is immutable
                      rule: self == oldSelf
                rotation:
                  description: Rotation defines timings when the service account should be recreated.
                  properties:
                    enabled:
                      default: false
                      type: boolean
                    rotation:
                      pattern: ^(@(annually|yearly|monthly|weekly|daily|hourly)|(((\d+,)*\d+|(\d+(\/|-)\d+)|\*)\s?){5,6})$
                      type: string
                  required:
                    - enabled
                  type: object
                  x-kubernetes-validations:
                    - message: rotation must be set if enabled is true
                      rule: 'self.enabled == true ? self.rotation.size() > 0 : true'
                secretRef:
                  description: |-
                    SecretRef defines the secret where the generated service account will be written to.
                    If this field is empty, the service account will be written to a secret named like the name of the serviceaccount request resource.
                    The final secret reference will be written to the status object.
                  properties:
                    name:
                      description: Name des Secrets.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?([.][a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                  required:
                    - name
                  type: object
              required:
                - consumer
                - consumerType
                - producer
              type: object
            status:
              description: status defines the observed state of ServiceAccountRequest.
              properties:
                conditions:
                  description: Conditions represent the current state of the ServiceAccountRequest resource.
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                lastRotation:
                  description: LastRotation defines the timestamp of the last rotation.
                  format: date-time
                  type: string
                secretRef:
                  description: SecretRef defines the actual secret where the service account was written to.
                  properties:
                    name:
                      description: Name des Secrets.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?([.][a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                  required:
                    - name
                  type: object
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
